## Summary

<!-- What this PR does and why — the problem or need it addresses. A
     one-paragraph "why" beats a bullet list of "what": the diff already
     shows what changed, so use this space for the motivation and any
     tradeoffs a reviewer wouldn't otherwise see. -->

## Verification

<!-- What was actually run/checked, not just "tests pass" — CI already
     covers `cargo test` and `tsc --noEmit` (see .github/workflows/ci.yml),
     so focus this on what CI can't see: manual/visual checks for
     rendering or input behavior, clippy/fmt if relevant, anything run
     by hand and why. -->

- [ ]

## Known limitations / follow-ups

<!-- Anything deliberately left out of scope, or a known rough edge —
     stated here so it reads as a decision, not something found later.
     Delete this section if there isn't one. -->
