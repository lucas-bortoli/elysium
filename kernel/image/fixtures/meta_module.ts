// A real file whose path exists purely so tests can canonicalize it —
// `runtime.rs`'s `import_meta_reports_virtual_userland_paths` test evaluates
// its own inline source under this file's name, not this file's contents.
